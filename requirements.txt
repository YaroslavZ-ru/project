# AI-Terminator -- зависимости проекта
# Установка: pip install -r requirements.txt

# Морфологический анализ русского языка
pymorphy3>=1.0.0
pymorphy3-dicts-ru>=2.4.417150

# fastText-модель для векторных представлений слов
# ВНИМАНИЕ: требует компилятор C++ (Microsoft Visual C++ Build Tools).
# Установить отдельно ПОСЛЕ установки Build Tools:
#   pip install fasttext-wheel>=0.9.2
# Скачать Build Tools: https://visualstudio.microsoft.com/visual-cpp-build-tools/
# fasttext-wheel>=0.9.2   # <- раскомментировать после установки Build Tools

# Тестирование
pytest>=7.0.0

# --- ОТКЛЮЧЕНО (будет включаться по мере развития проекта) ---
# faiss-cpu>=1.7.0           # быстрый поиск по векторам (изм.17)
# transformers>=4.30.0       # генеративный модуль (изм.21+)
# torch>=2.0.0               # бэкенд для transformers (изм.21+)
# transliterate>=1.10.2      # транслитерация (если потребуется)
# fastapi>=0.100.0           # REST API (если потребуется)
# uvicorn>=0.23.0            # ASGI-сервер для FastAPI (если потребуется)
