# AI-Terminator

Интеллектуальный помощник: по термину и подсказкам определяет контекст и возвращает параметры для описания.

```
вход:  термин="ключ"  +  hints=["техника"]
выход: домен="слесарный инструмент",  параметры: material, size_mm, torque_nm...

вход:  термин="ключ"  +  hints=["музыка"]
выход: домен="музыка",  параметры: clef_type, staff_position...
```

---

## Быстрая установка (Windows)

1. Скачайте и установите **Python 3.12**: https://www.python.org/downloads/
   - При установке отметьте **"Add Python to PATH"**

2. Скачайте и установите **Node.js 18+**: https://nodejs.org/
   - При установке отметьте **"Add to PATH"**

3. Скачайте этот репозиторий

4. Запустите **setup.bat** (двойной клик)
   - Установит Python зависимости (~5 мин)
   - Установит Node.js зависимости для веб-приложения
   - Скачает модель FastText (~8 ГБ, ~15 мин)
   - Инициализирует базу данных (86 понятий, 14 доменов)

5. Запустите **run.bat** (двойной клик)
   - Выберите режим: API, CLI, веб-интерфейс или тесты

---

## Установка вручную

```bash
# 1. Создать виртуальное окружение
py -3.12 -m venv venv312
venv312\Scripts\activate

# 2. Установить зависимости
pip install -r requirements.txt
pip install fastapi uvicorn httpx
pip install fasttext-wheel

# 3. Скачать модель FastText (~8 ГБ)
# Скачать cc.ru.300.bin.gz с https://fasttext.cc/docs/en/crawl-vectors.html
# Распаковать в models/cc.ru.300.bin

# 4. Инициализировать БД (86 понятий)
python setup_project.py
python -m scripts.setup_all --force
```

---

## Запуск

```bash
# Активировать окружение
venv312\Scripts\activate

# REST API
python -m scripts.run_api              # http://127.0.0.1:8000

# CLI (один запрос)
echo '{"term":"ключ","hints":["техника"]}' | python main.py --once

# CLI (интерактивный)
python main.py

# Веб-интерфейс (автоматически запускает API + Web)
start_web.bat                          # http://localhost:3000

# Перезапуск с свежим кодом
restart_all.bat
```

---

## Пример вывода

```json
{
  "status": "ok",
  "term": "стол",
  "selected_context": {
    "domain": "мебель",
    "confidence": 0.64
  },
  "parameters": [
    {
      "name": "material",
      "label_ru": "Материал",
      "type": "enum",
      "description": "Основной материал изготовления",
      "enum_values": ["дерево", "металл", "пластик", "стекло", "камень", "композит"],
      "confidence": 1.0,
      "source": "knowledge_base"
    },
    {
      "name": "wood_type",
      "label_ru": "Порода дерева",
      "type": "enum",
      "description": "Порода древесины для деревянных столов",
      "enum_values": ["сосна", "ель", "дуб", "бук", "ясень", "орех", "вишня", "клен", "тик"],
      "confidence": 0.88,
      "source": "knowledge_base"
    }
  ],
  "suggested_refinements": [
    "Уточните тип: обеденный, письменный, журнальный, компьютерный, кухонный, консольный",
    "Уточните материал: дерево, металл, пластик, стекло, камень, композит"
  ],
  "warnings": []
}
```

---

## API

| Метод | Путь | Описание |
|---|---|---|
| POST | `/v1/query` | Анализ термина |
| POST | `/v1/save_concept` | Добавить понятие в БД |
| POST | `/v1/feedback` | Оценить результат (1-5) |
| GET | `/v1/health` | Проверка здоровья |
| GET | `/v1/metrics` | Метрики Prometheus |
| GET | `/v1/kb/stats` | Статистика БД |
| GET | `/docs` | Swagger UI |

---

## Веб-интерфейс

Графический интерфейс для работы с AI-Terminator через браузер.

### Запуск

```bash
# Способ 1: двойной клик (рекомендуется)
start_web.bat

# Способ 2: PowerShell
.\start_web.ps1

# Способ 3: вручную
cd web && npm install && node server.js
```

Скрипт `start_web.bat` автоматически:
- Останавливает старые процессы API и Web
- Запускает Python backend (port 8000)
- Запускает Node.js веб-сервер (port 3000)
- Открывает браузер

### Возможности

- **Анализ термина** — вводите термин + подсказки, скачиваете JSON с параметрами
- **Выбор домена** — при неоднозначности показываются варианты, клик выбирает домен
- **Экспорт** — скачайте результат в JSON или CSV
- **Обратная связь** — оцените результат звёздами (1-5)
- **Статистика** — количество понятий и параметров в БД

### Архитектура

```
Браузер (localhost:3000)
    ↓ HTTP
Express.js (web/server.js)  ← проксирует запросы
    ↓ HTTP
FastAPI backend (localhost:8000)
```

---

## База знаний

| Домен | Понятий | Примеры |
|---|---|---|
| слесарный инструмент | 11 | ключ гаечный, молоток, отвёртка |
| электроника | 9 | резистор, конденсатор, диод |
| музыка | 8 | гитара, пианино, скрипка |
| крепёж | 8 | болт, гайка, саморез |
| мебель | 6 | стол, стул, шкаф, кровать |
| бытовая техника | 6 | холодильник, стиральная машина |
| строительство | 6 | кирпич, бетон, арматура |
| спорт | 5 | мяч, ракетка, лыжи |
| одежда | 4 | куртка, джинсы, обувь |
| еда | 5 | кофе, чай, шоколад |
| животные | 4 | собака, кошка, рыба |
| медицина | 4 | термометр, бинт, шприц |
| оборудование | 5 | сверло, перфоратор, болгарка |
| транспорт | 4 | автомобиль, велосипед |

**Итого: 86 понятий, 323 параметра, 14 доменов**

---

## Требования

| Компонент | Минимум | Рекомендуется |
|---|---|---|
| Python | 3.12 | 3.12 |
| Node.js | 18 | 20+ |
| RAM | 4 ГБ | 8 ГБ |
| Диск | 10 ГБ | 20 ГБ |

**Python (обязательные):** pymorphy3, numpy, fasttext-wheel

**Опциональные:**
- `faiss-cpu` — быстрый поиск при >10k понятий
- `transformers`, `torch` — генеративное расширение параметров
- `prometheus_client` — метрики

---

## Тесты

```bash
venv312\Scripts\activate
python -m pytest tests/ -v     # 229 тестов, 0 падений
```

---

## Конфигурация

| Файл | Назначение |
|---|---|
| `configs/config.json` | Основной конфиг |
| `configs/development.json` | Override для dev |
| `configs/production.json` | Override для prod |
| `configs/domain_keywords.json` | Ключевые слова для определения домена |
| `configs/domain_templates.json` | Шаблоны параметров для fallback |

Запуск с окружением: `python -m scripts.run_api --env production`

---

## Структура проекта

```
project/
├── src/                    # Ядро системы
│   ├── config.py          # Конфигурация
│   ├── preprocess.py      # Предобработка текста
│   ├── embeddings.py      # FastText эмбеддинги
│   ├── vectorize.py       # Векторизация запросов
│   ├── knowledge_base.py  # Поиск в БД
│   ├── aggregation.py     # Агрегация параметров
│   ├── fallback.py        # Резервный режим
│   ├── generative.py      # Генеративное расширение (LLM)
│   ├── sessions.py        # Управление сессиями
│   ├── metrics.py         # Метрики Prometheus
│   ├── observability.py   # Трассировка запросов
│   ├── utils.py           # Утилиты
│   └── api.py             # REST API
├── web/                   # Веб-интерфейс
│   ├── server.js          # Express-сервер + прокси
│   └── public/            # Статика (HTML, CSS, JS)
├── scripts/               # Утилиты
│   ├── init_db.py         # Инициализация БД
│   ├── seed_massive.py    # Наполнение БД (86 понятий)
│   ├── setup_all.py       # Полная инициализация
│   ├── run_api.py         # Запуск API
│   └── ...
├── tests/                 # Тесты (229 шт)
├── configs/               # Конфигурация
├── data/                  # БД и данные
├── models/                # Модели (cc.ru.300.bin)
├── AI/                    # Описание, планы, промпты
├── setup.bat              # Скрипт установки
├── run.bat                # Скрипт запуска
├── start_web.bat          # Запуск веб-интерфейса
└── restart_all.bat        # Перезапуск с свежим кодом
```
